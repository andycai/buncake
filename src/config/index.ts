/**
 * 框架配置
 */
export default {
  // 应用名称
  appName: 'BunCake',
  
  // 环境配置
  env: process.env.NODE_ENV || 'development',
  
  // 日志级别
  logLevel: process.env.LOG_LEVEL || 'info',
  
  // 其他配置项可以根据需要添加
};